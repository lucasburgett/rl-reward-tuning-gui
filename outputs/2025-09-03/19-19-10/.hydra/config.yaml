seed: 42
deterministic: true
device: auto
total_steps: 1000
log_dir: experiments/${now:%Y-%m-%d}/${now:%H-%M-%S}
use_wandb: false
eval:
  run_after_train: false
  n_episodes: 10
env: cartpole
algo: ppo
